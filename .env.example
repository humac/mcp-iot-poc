# Home Assistant Configuration
HA_URL=http://your-homeassistant-ip:8123
HA_TOKEN=your_long_lived_access_token_here
HA_ENTITY_ID=climate.your_thermostat

# Ollama Configuration
OLLAMA_URL=http://your-ollama-ip:11434
# Recommended models for native tool calling (requires Ollama tool API support):
#   - ministral-3:14b   (10GB VRAM, excellent tool calling, designed for agents) ✓ RECOMMENDED
#   - ministral-3:8b    (6GB VRAM, good balance for tool calling) ✓ Tool calling
#   - llama3.1:8b       (8GB VRAM, good balance of speed and quality) ✓ Tool calling
#   - llama3.1:70b      (40GB+ VRAM, best reasoning but slow) ✓ Tool calling
#   - qwen2.5:14b       (10GB VRAM, excellent tool calling, may respond in Chinese) ✓ Tool calling
#   - mistral:7b        (6GB VRAM, fast but less reliable tool use) ✓ Tool calling
# Models that do NOT support native tool calling (will get 400 errors):
#   - gemma3:12b        (Does NOT support Ollama tool API)
#   - gemma2:9b         (Does NOT support Ollama tool API)
OLLAMA_MODEL=ministral-3:14b

# Agent Configuration
CHECK_INTERVAL_MINUTES=30
MIN_TEMP=17
MAX_TEMP=23

# MCP Server URLs (for Docker networking, defaults work out of the box)
# Only change these if running MCP servers on different hosts
WEATHER_MCP_URL=http://weather-mcp:8080
HA_MCP_URL=http://homeassistant-mcp:8080

# Location (for weather forecasts)
LATITUDE=45.42
LONGITUDE=-75.69
TIMEZONE=America/Toronto

# Database Configuration
# Path to SQLite database for storing decisions and settings
DB_PATH=/app/data/decisions.db

# Debug Mode
# Set to "true" to enable debug mode in MCP servers (verbose errors, stack traces)
# WARNING: Do not enable in production - exposes internal details
DEBUG=false

# Dashboard Authentication (optional)
# Leave blank to disable authentication (dashboard accessible without login)
# DASHBOARD_USER=admin
# DASHBOARD_PASS=your_secure_password_here

# Logging Format
# Set to "json" for structured JSON logging (production), "text" for human-readable (development)
LOG_FORMAT=text

# MCP Server Authentication (optional)
# If set, MCP servers will require this token in Authorization: Bearer header
# This secures inter-service communication between agent and MCP servers
# MCP_AUTH_TOKEN=your_secret_mcp_token_here
